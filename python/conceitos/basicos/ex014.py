# Criar um programa que converta graus celcios para farenhight

c = float(input('Insira a temperatura: °C'))

result = (c * 9 / 5) + 32

print('A conversão de {}°C para fahrenheit é de: {}f'.format(c, result))
